#pragma once

template<class T> class CBTArr {
private:
  static int Max = 20;
  T arr[Max]{};
  T sum[Max]{};
  int len = 0;

public:
  void insert(const T &x) {
    int p = len;
    while (p <= )
  }
};